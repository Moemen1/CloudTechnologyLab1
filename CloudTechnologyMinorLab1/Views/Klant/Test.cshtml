@model Dictionary<string, object>

@{
    ViewData["Title"] = "Test";
}

<h2>Test</h2>

<div>
    @foreach (var item in Model.Values)
    {
        <tr>
            <td>
                @item
            </td>
        </tr>
    }
</div>
